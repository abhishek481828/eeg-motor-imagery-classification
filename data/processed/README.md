# Processed Data Directory

This directory stores intermediate preprocessed window arrays and normalization scalers.

> [!NOTE]
> Processed arrays are generated automatically by `python scripts/preprocess_dataset.py` and are excluded from Git via `.gitignore`.
